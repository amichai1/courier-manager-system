<Window x:Class="PL.Courier.CourierListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Courier"
        xmlns:pl="clr-namespace:PL"
        mc:Ignorable="d"
        Title="Courier List" Height="640" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Loaded="Window_Loaded"
        Closed="Window_Closed">

    <Border Background="{StaticResource BackgroundColor}" 
            CornerRadius="8"
            Margin="10" 
            Padding="15">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="20" ShadowDepth="2"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,10">
                <TextBlock Text="Couriers List" Style="{StaticResource SectionHeader}"/>
                <Rectangle Height="2" Fill="{StaticResource PrimaryColor}" HorizontalAlignment="Left" Width="60"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="Filter by Delivery Type:" Style="{StaticResource LabelHeader}"/>
                <ComboBox Grid.Column="1" Width="200" 
                          Style="{StaticResource ComboBoxRegular}"
                          ItemsSource="{Binding Source={StaticResource DeliveryTypesCollectionKey}}"
                          SelectedValue="{Binding DeliveryTypeFilter, Mode=TwoWay}"
                          SelectionChanged="ComboBox_SelectionChanged"/>
            </Grid>

            <DataGrid Grid.Row="2" 
                      ItemsSource="{Binding CourierList}" 
                      AutoGenerateColumns="False"
                      CanUserAddRows="False" 
                      IsReadOnly="True" 
                      SelectedItem="{Binding SelectedCourier}"
                      MouseDoubleClick="DataGrid_MouseDoubleClick">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*" MinWidth="40"/>

                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />

                    <DataGridTextColumn Header="Type" Binding="{Binding DeliveryType}" Width="*" />

                    <DataGridTextColumn Header="On Time" Binding="{Binding DeliveredOnTime}" Width="*" />
                    <DataGridTextColumn Header="Late" Binding="{Binding DeliveredLate}" Width="*" />

                    <DataGridTextColumn Header="Current Order" Binding="{Binding CurrentIdOrder, TargetNullValue='-'}" Width="*" />

                    <DataGridTemplateColumn Header="Active" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsActive, Mode=OneWay}" 
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Click="CheckBox_Click"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Actions" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Delete" 
                                        Style="{StaticResource DeleteButtonStyle}"
                                        Click="DeleteButton_Click"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Refresh List" Click="btnRefresh_Click" Style="{StaticResource SecondaryButton}" Width="120"/>
                <Button Content="Add New Courier" Click="btnAdd_Click" Style="{StaticResource PrimaryButton}" Width="160"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
