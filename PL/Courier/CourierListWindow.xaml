<Window x:Class="PL.Courier.CourierListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Courier"
        xmlns:pl="clr-namespace:PL"
        xmlns:converters="clr-namespace:PL.Converters"
        mc:Ignorable="d"
        Title="WPF Manage" Height="680" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource AppBackgroundColor}"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Loaded="Window_Loaded"
        Closed="Window_Closed">

    <Window.Resources>
        <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <converters:BoolToAvailabilityConverter x:Key="BoolToAvailabilityConverter"/>
        <converters:IsraeliIdDisplayConverter x:Key="IsraeliIdDisplayConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Add New Courier Button -->
            <Button Grid.Column="0" 
                    Content="+ Add New Courier" 
                    Click="btnAdd_Click"
                    Style="{StaticResource AddButtonStyle}"/>

            <!-- Title and Filter/Sort -->
            <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                <TextBlock Text="Courier Management" Style="{StaticResource PageTitleStyle}"/>
                
                <!-- Filter and Sort Controls -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <!-- Filter by Status -->
                    <TextBlock Text="Filter by Status" Style="{StaticResource FilterLabelStyle}"/>
                    <ComboBox Width="150" 
                              Style="{StaticResource ComboBoxRegular}"
                              ItemsSource="{Binding Source={StaticResource DeliveryTypesCollectionKey}}"
                              SelectedValue="{Binding DeliveryTypeFilter, Mode=TwoWay}"
                              SelectionChanged="ComboBox_SelectionChanged"/>

                    <!-- Sort By -->
                    <TextBlock Text="Sort By" Style="{StaticResource FilterLabelStyle}" Margin="20,0,10,0"/>
                    <ComboBox x:Name="cmbSortBy"
                              Width="150" 
                              Style="{StaticResource ComboBoxRegular}"
                              ItemsSource="{pl:CourierSortByCollection}"
                              SelectedValue="{Binding SelectedSortBy, Mode=TwoWay}"
                              SelectionChanged="SortComboBox_SelectionChanged"/>

                    <!-- Sort Order -->
                    <TextBlock Text="Order" Style="{StaticResource FilterLabelStyle}" Margin="20,0,10,0"/>
                    <ComboBox x:Name="cmbSortOrder"
                              Width="120" 
                              Style="{StaticResource ComboBoxRegular}"
                              ItemsSource="{pl:SortOrderCollection}"
                              SelectedValue="{Binding SelectedSortOrder, Mode=TwoWay}"
                              SelectionChanged="SortComboBox_SelectionChanged"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Courier Cards List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding CourierList}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}" MouseLeftButtonDown="CourierCard_Click">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Delete Button -->
                                <Button Grid.Column="0" 
                                        Content="🗑 Delete" 
                                        Click="DeleteButton_Click"
                                        Style="{StaticResource DangerButtonStyle}"
                                        Margin="0,0,20,0"
                                        VerticalAlignment="Center"/>

                                <!-- Availability Toggle -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,30,0">
                                    <TextBlock Text="Availability" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <CheckBox IsChecked="{Binding IsActive, Mode=OneWay}" 
                                                  Style="{StaticResource ToggleSwitchStyle}"
                                                  Click="CheckBox_Click"
                                                  VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding IsActive, Mode=OneWay, Converter={StaticResource BoolToAvailabilityConverter}}" 
                                                   VerticalAlignment="Center" Margin="8,0,0,0"
                                                   Foreground="{StaticResource LightTextColor}"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Order Statistics -->
                                <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock HorizontalAlignment="Center">
                                        <Run Text="Current Order ID: " FontWeight="SemiBold"/>
                                        <Run Text="{Binding CurrentIdOrder, Mode=OneWay, TargetNullValue='-'}"/>
                                    </TextBlock>
                                    <TextBlock HorizontalAlignment="Center">
                                        <Run Text="On-Time Deliveries: "/>
                                        <Run Text="{Binding DeliveredOnTime, Mode=OneWay}"/>
                                    </TextBlock>
                                    <TextBlock HorizontalAlignment="Center">
                                        <Run Text="Late Deliveries: "/>
                                        <Run Text="{Binding DeliveredLate, Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>

                                <!-- Status and Location -->
                                <StackPanel Grid.Column="3" VerticalAlignment="Center" Margin="20,0" HorizontalAlignment="Center">
                                    <!-- Status Badge -->
                                    <Border Style="{StaticResource StatusBadgeStyle}" 
                                            Background="{Binding IsActive, Mode=OneWay, Converter={StaticResource StatusToColorConverter}, ConverterParameter=CourierStatus}"
                                            Margin="0,0,0,8">
                                        <TextBlock Text="{Binding IsActive, Mode=OneWay, Converter={StaticResource BoolToAvailabilityConverter}}" 
                                                   Foreground="White" FontWeight="SemiBold" FontSize="12"/>
                                    </Border>
                                    <!-- Location -->
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="📍" FontSize="14" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding DeliveryType, Mode=OneWay}" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Courier Info -->
                                <StackPanel Grid.Column="4" VerticalAlignment="Center" HorizontalAlignment="Right" MinWidth="180">
                                    <TextBlock HorizontalAlignment="Right" FontWeight="SemiBold">
                                        <Run Text="ID: "/>
                                        <Run Text="{Binding Id, Mode=OneWay, Converter={StaticResource IsraeliIdDisplayConverter}}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding Name, Mode=OneWay}" HorizontalAlignment="Right" FontSize="14"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
